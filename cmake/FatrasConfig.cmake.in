# - Config file for the Fatras package
#
# It defines the `Fatras` cmake target and nothing else. Include directories
# and library dependencies are handled exclusively via target properties.

# find the ACTS package
if(Fatras_FIND_REQUIRED)
  find_package(ACTS REQUIRED COMPONENTS Core)
else()
  find_package(ACTS COMPONENTS Core)
endif()

# CMakePackageConfigHelpers initialization
@PACKAGE_INIT@

if(NOT Fatras_FIND_QUIETLY)
  message(STATUS "Found Fatras version @PROJECT_VERSION@")
endif()

include("${CMAKE_CURRENT_LIST_DIR}/FatrasTargets.cmake")
